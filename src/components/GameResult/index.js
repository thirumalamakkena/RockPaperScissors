import './index.css'

const GameResult = props => {
  const {onClickPlayAgain, optedChoice, opponentChoice, result} = props

  const onClickButton = () => {
    onClickPlayAgain()
  }

  return (
   
  )
}

export default GameResult
